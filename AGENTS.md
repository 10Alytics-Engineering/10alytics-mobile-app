# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Expo UI
Expo UI is a collection of React Native components and utilities that are designed to work seamlessly with Expo projects. It provides a set of pre-built components that can be easily customized and used in your Expo applications.

Read the documentation at https://docs.expo.dev/versions/v56.0.0/sdk/ui/
